import java.util*

class Find {
	public static void findMinMax(int [] number, int [] result){
		int min=Integer.MIN_VALUE
		int max=Integer.MAX_VALUE

	for (int num:numbers)
		if (num<min) {
			min=num
		}
		if (num>min) {
			max=min
		}
	}

	result[0]=min
	result[1]=max
}

public static void main (String args[]) {
	Scaner sc=new System (System.in)
	s.o.p ("enter le nombre element")
	int n = sc.nextInt()
	for (i=0, i<n, i++) {
		number[i]=sc.nextInt()
	}
	int result
}